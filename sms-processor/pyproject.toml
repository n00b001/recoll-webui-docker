[project]
name = "sms-processor"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "n00b001", email = "xfanth@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
sms-processor = "sms_processor:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.1",
    "ty>=0.0.67",
]
